# ============================================================
# Audiobookshelf 配置（systemd EnvironmentFile）
# 改完执行: systemctl restart audiobookshelf
#
# 安全敏感项（监听地址/端口/URL 前缀/数据目录）已写死在 systemd unit 的
# ExecStart flags 与 Environment= 里，不在此配置——防止 env 误删/漏配时
# 端口外泄或改变监听地址（navidrome 模式；详见单元文件内注释）。
# 完整变量列表: https://www.audiobookshelf.org/docs/environment-variables
# ============================================================

# ---- 可选：时区（默认跟随系统；仅在应用时间显示异常时设置）----
# TZ=Asia/Shanghai

# ---- 可选：允许 CORS 请求（默认关闭；仅特殊客户端需要）----
# ALLOW_CORS=1

# ---- 可选：备份目录（默认 /var/lib/audiobookshelf/config/backups）----
# BACKUP_PATH=/var/lib/audiobookshelf/config/backups

# ---- 可选：ffmpeg 走 nginx X-Accel-Redirect 加速大文件（默认关闭；
#      开启还需在 nginx 配置 internal location，本包未内置）----
# USE_X_ACCEL=1
